# Reel Factory V1
Automated Short-Story Reel Generator.

## Setup
1. Install dependencies: `uv pip install .`
2. Configure `.env` using `.env.example`.
3. Run doctor: `reel-factory doctor`.

## PoC Run
`reel-factory run-daily --date 2026-07-23 --dry-run`
